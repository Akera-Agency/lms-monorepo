{
  "name": "@akera/supabase",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docker:dev": "docker compose -f docker-compose.yml -f ./dev/docker-compose.dev.yml up -d",
    "docker": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:destroy": "docker compose -f docker-compose.yml -f ./dev/docker-compose.dev.yml down -v --remove-orphans",
    "docker:reset": "./reset.sh"
  }
}
